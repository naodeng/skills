# AGENTS.md

本文件是 `skills` 仓库的项目级协作约定。它适用于仓库根目录下所有 Skill；距离正在编辑文件更近的子目录级 `AGENTS.md` 可以补充更具体的规则。

## 项目定位

这是一个以 Markdown 为主的 Codex/Agent Skill 集合，不是单一可执行应用。每个 Skill 应尽量能够独立阅读、复制和使用。

## 目录与文件职责

通常一个 Skill 放在独立的 kebab-case 目录中：

```text
<skill-name>/
├── SKILL.md                 # 必需：入口说明与执行规范
├── README.md                # 可选：面向维护者或用户的简要说明
├── agents/openai.yaml       # 可选：Codex 展示和调用元数据
├── references/              # 可选：按需加载的深度资料
├── examples/                # 可选：可复用示例
└── scripts/                 # 可选：可执行辅助工具
```

编辑前先确认目标 Skill 的实际结构和相邻说明文件，不要因为一个 Skill 的结构而假设所有 Skill 都相同。

## Skill 编写规范

- `SKILL.md` 必须包含有效 YAML frontmatter，至少有 `name` 和 `description`。
- `name` 使用小写字母、数字和连字符，并与目录名保持一致。
- `description` 只描述触发条件；推荐以 `Use when` 开头，不要把完整工作流塞进描述字段。
- 入口文件保持精简、可执行；长参考资料、示例和故障排查放入支持文件。
- 保持主题和技术术语准确，不要伪造 API、命令、版本、链接或验证结果。
- 双语 Skill 应保持目录结构、主要流程和关键约束对齐；自然语言可翻译，但代码、命令、路径、标识符和 URL 默认保持不变。
- 不在 Skill 文档或示例中写入真实 token、密码、cookie、私钥、个人数据或其他秘密。
- 修改一个 Skill 的核心契约时，同步检查其 `README.md`、`references/`、`examples/`、`agents/openai.yaml` 和相关脚本。

## 新增与修改流程

1. 先检查当前目录、项目说明、Git 状态和目标 Skill 的完整结构。
2. 明确用户要求的范围；不要顺手重构无关 Skill。
3. 对新结构或较大改动先写清楚设计和边界，再实现。
4. 修改后检查 frontmatter、相对链接、文件引用和跨文件一致性。
5. 运行与改动匹配的验证命令，再汇报实际结果和未覆盖的风险。

## 验证要求

优先使用仓库已有的验证器和脚本。若没有统一验证器，至少执行适用的检查：

```bash
git diff --check
git status --short
```

对 Skill 文档还应检查：

- frontmatter 可以解析，`name` 与目录名一致；
- `SKILL.md` 引用的文件真实存在；
- Markdown 相对链接可以解析；
- 示例中的命令、路径和输出没有明显自相矛盾；
- 生成物、缓存和临时文件没有混入提交范围。

静态检查不能证明 Skill 在真实 Agent 对话中的行为，也不能证明浏览器渲染、远程部署或外部服务状态；报告时要区分这些验证级别。

## Git 与工作区安全

- 默认保留用户已有的未提交修改；先看 `git status --short`，不要擅自覆盖或回退。
- 只修改当前任务涉及的文件，不删除、重置或清空宽泛目录。
- 未经用户明确要求，不创建提交、不推送远程、不修改 Git 配置。
- 提交前只暂存任务相关路径，并再次运行 `git diff --check`。

## 交付汇报

汇报应简明说明：改了什么、为什么这样改、运行了哪些检查、哪些内容尚未验证。不要把“代码已修改”表述成“运行时已验证”，也不要在未检查远程状态时声称已发布。
